{
	"name": "Webshop",
	"dockerComposeFile": "docker-compose.yml",
	"service": "webshop",
	"workspaceFolder": "/workspace",
	"postCreateCommand": "pnpm install --frozen-lockfile",
	"postStartCommand": "bash -lc 'if [[ ! -f node_modules/.pnpm/lock.yaml || pnpm-lock.yaml -nt node_modules/.pnpm/lock.yaml || package.json -nt node_modules/.pnpm/lock.yaml ]]; then pnpm install --frozen-lockfile; fi'",
	"forwardPorts": [3000],
	"portsAttributes": {
		"3000": {
			"label": "Webapp"
		}
	}
}